# 整理记录

快照日期：2026-09-04。

## 收录范围

- 从个人 Codex 技能目录收录 9 个完整技能包和 `_shared`，保留原名称与引用关系。
- 从已有论文协作配置提炼写作、编码、检查、审核 4 个角色及交接流程。
- 将全局论文存储与工作规则整理为可选模板，移除此前固定绑定的论文项目。
- 将系统技能与插件缓存中的技能记录为依赖目录；插件缓存存在不等于当前启用。
- 已检查常见 Codex、Claude 与本地 Agent 配置位置；没有将聊天记录或工作项目整体作为 Agent 定义导出。

## 公开副本的调整

- Agent 使用项目变量和相对工作目录，移除原始稿件路径、具体研究命题、实验参数及实际诊断。
- PDF 技能的个人 `uv.exe` 绝对路径改为 `%LOCALAPPDATA%` 示例。
- Nature 写作示例的长篇原文整理为结构分析、填写骨架与原笔记中的来源线索；保持文件路径，避免破坏教学引用。
- 保留 `sync-overleaf-local` 的 `E:\Overleaf` 约定，安装说明明确 Windows 限制。
- Overleaf 初始化脚本在首次写入前检查将生成的配置、同步状态、AGENTS 和构建文件；发现同名文件即停止，避免重跑覆盖已有项目配置。
- UTF-8 文本统一 LF，保留许可证。没有合并名称相似但用途不同的技能。

## 不在公开仓库的内容

凭据、账号授权、原始 `config.toml` / `settings.json`、会话与数据库、运行日志、插件缓存实现、官方专有模板资产、实际稿件和研究数据。

原有安装目录、Agent 工作目录和全局配置均保留。此仓库是经过整理的发布副本，不与原目录自动双向同步。

## 后续更新

1. 更新相关技能或角色文件，保留来源和许可证。
2. 如增删技能，同步 `catalog/skills.json` 和 README 分类表；共享内容仍放 `_shared`。
3. 执行 `python scripts/validate.py`，检查结构、静态引用、文本格式、Python 语法与常见凭据模式。
4. 审阅 `git diff` 后提交，再按自己的发布意图推送。

自动检查验证的是包结构，不等同于所有 Skill 的真实场景测试。需要连接器、图像生成、LaTeX 或外部服务的流程应在对应环境分别验证。
